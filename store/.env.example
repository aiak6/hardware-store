# Copy to .env (or export these) before starting.
# A NEW database, separate from the investment app. Do not reuse that DB.
PORT=8080
DATABASE_URL=postgresql://storeuser:CHANGE_ME@localhost:5432/rack_and_reason
